import 'package:flutter/material.dart';

class GestureExp extends StatelessWidget {
  const GestureExp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}